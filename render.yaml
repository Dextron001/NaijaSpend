services:
  - type: web
    name: naijaspend
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: naijaspend-data
      mountPath: /app/server/data
      sizeGB: 1
